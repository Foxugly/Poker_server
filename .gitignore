# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Django
db.sqlite3
db.sqlite3-*
/staticfiles/
/media/
*.env

# Secrets / local
/run/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Celery beat scheduler state (runtime, regenerated by the beat process)
celerybeat-schedule
celerybeat-schedule.*
celerybeat.pid

# Secrets : `.env` seul ne couvre que le nom exact. Un `.env.e2e-backup`
# porteur de SECRET_KEY / DB_PASSWORD a ete trouve non ignore sur
# trainingmanager_server le 2026-07-31 — un `git add -A` l'aurait commite.
.env
.env.*
